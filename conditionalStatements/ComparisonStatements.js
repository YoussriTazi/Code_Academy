//if the hunger level is greater than 7 we should eat
let hungerLevel = 7
if (hungerLevel > 7) {
    console.log('Time to eat!')
//Otherwise we can eat later
} else {
    console.log ('We can eat later')
}

// if the hunger level is not 7 we should eat 
hungerLevel = 10
if (hungerLevel !== 7) {
    console.log('Time to eat!')
//Otherwise we can eat later
} else {
    console.log ('We can eat later')
}